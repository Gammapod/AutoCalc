{
  "name": "autocalc-v1-slice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json",
    "dev:serve": "node ./scripts/dev-server.mjs",
    "test": "npm run build && node ./dist/tests/run-tests.js"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "algebrite": "^1.4.0"
  }
}
