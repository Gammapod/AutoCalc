# v0.8.2 Test-First Matrix

Purpose: define the tests to write/update before feature implementation.  
Scope: `step_through` + `[ ??? ]` only.

## 1. Domain Scenarios

| ID | Scenario | Target Suite(s) | Expected Assertions |
|---|---|---|---|
| D-01 | Step session starts on first `step_through` and executes one slot | `tests/reducer.input.test.ts` | `nextSlotIndex=1`, one executed result captured, roll unchanged |
| D-02 | Repeated `step_through` advances cursor one slot per press | `tests/reducer.input.test.ts` | cursor increments by exactly 1 each press |
| D-03 | Terminal step completion commits exactly one roll append | `tests/reducer.input.test.ts` | roll length +1 terminal step row, no intermediate rows |
| D-04 | `=` after partial step continues from cursor, not slot 0 | `tests/reducer.input.test.ts` | output equals suffix execution from current partial total |
| D-05 | `step_through` with zero committed slots is no-op | `tests/reducer.input.test.ts` | calculator and roll unchanged |
| D-06 | Error during stepped slot finalizes once and resets step state | `tests/reducer.input.test.ts` | terminal error row appended once, step inactive |
| D-07 | Clear actions cancel step session (`C`, `UNDO`, `←`) | `tests/reducer.input.test.ts` | step state reset to inactive |
| D-08 | Layout transitions cancel step session | `tests/reducer.layout.test.ts` | any move/swap/dimension action resets step state |
| D-09 | Step state is inert when step key absent from keypad | `tests/reducer.input.test.ts` | session cleared/disabled and no step-only behavior applies |
| D-10 | Toggle exclusivity includes `[ ??? ]` and existing settings toggles | `tests/reducer.lifecycle.test.ts` | setting one clears other settings flags |

## 2. UI + Read-Model Scenarios

| ID | Scenario | Target Suite(s) | Expected Assertions |
|---|---|---|---|
| U-01 | White slot-token highlight appears only with keypad presence of `step_through` | `tests/uiIntegration.mobileShell.test.ts`, `tests/uiIntegration.desktopShell.test.ts` | active slot token has white-highlight class only when step key is on keypad |
| U-02 | Active-step target index drives slot-token highlight location | `tests/operationSlotDisplay.test.ts` and shell integration tests | highlighted token index equals `nextSlotIndex` (or `0` when inactive) |
| U-03 | Executed slots render inline replacement `[ -> value ]` | `tests/operationSlotDisplay.test.ts` | token substitution for executed slots only |
| U-04 | `[ ??? ]` active with expansion text replaces current target token | `tests/operationSlotDisplay.test.ts` | target token shows expansion text |
| U-05 | `[ ??? ]` active with no expansion uses fallback token | `tests/operationSlotDisplay.test.ts` | unchanged base token when resolver returns null |
| U-06 | `[ ??? ]` does nothing when no valid step target | `tests/operationSlotDisplay.test.ts` | no display delta vs baseline |

## 3. Integration + Parity Scenarios

| ID | Scenario | Target Suite(s) | Expected Assertions |
|---|---|---|---|
| I-01 | Mobile shell repeated `step_through` interaction | `tests/uiIntegration.mobileShell.test.ts` | sequence produces deterministic cursor+display progression |
| I-02 | Desktop shell mixed `step_through` + `=` interaction | `tests/uiIntegration.desktopShell.test.ts` | continuation semantics preserved across shells |
| I-03 | Persist/hydrate active step session | `tests/persistence.test.ts` | persisted step fields survive round-trip |
| I-04 | Hydrate invalid step payload coerces safely to inactive | `tests/persistence.test.ts` | malformed/invalid step state normalizes to defaults |
| I-05 | Parity fixtures include step state and toggle flag shape | `tests/contracts/parity*.test.ts` + fixtures | no schema drift and deterministic parity pass |

## 4. Traceability Gate

Each milestone behavior for v0.8.2 must map to:

- one spec clause in `docs/v0.8.2-execution-spec.md`
- one scenario ID in this file.

Required pass condition before implementation starts:

- all IDs above are either marked `Planned` with owning suite path, or already `Implemented` with passing assertions.
