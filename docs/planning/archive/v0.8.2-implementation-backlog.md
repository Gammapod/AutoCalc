# v0.8.2 Implementation Backlog

Purpose: execution-ready sequence for implementing `step_through` + `[ ??? ]` from locked pre-work specs.

## 1. Ordered Work Plan

## Task 1: Types, State, and Action Surface

Deliver:

- Add new key IDs: `exec_step_through`, `toggle_step_expansion`.
- Extend execution unlock typing to include `exec_step_through`.
- Add `STEP_EXPANSION_FLAG`.
- Add `StepProgressState` to calculator state and default/reset constructors.
- Ensure save/hydrate typing includes new step state.

Done criteria:

- TypeScript compiles with no type errors for new fields.
- Existing tests compile without fixture shape breakage.

Blockers:

- None.

## Task 2: Reducer Execution Path

Deliver:

- Add handler route for `step_through`.
- Implement step session lifecycle (start/advance/finalize/reset).
- Implement `=` continuation from partial step session.
- Implement step reset on cancel actions (clear, layout, reset/hydrate).
- Enforce keypad-presence rule for step enablement.

Done criteria:

- Domain scenario IDs `D-01` through `D-09` pass.
- No regressions in existing `=` behavior when step key absent.

Blockers:

- Task 1 complete.

## Task 3: Key Catalog, Presentation, and Toggle Exclusivity

Deliver:

- Add key catalog entries and presentation mappings for `▷` and `[ ??? ]`.
- Wire `[ ??? ]` as toggle flag key.
- Update exclusive settings group to include `STEP_EXPANSION_FLAG`.

Done criteria:

- New keys resolve correctly in key catalog validation and label rendering.
- Scenario `D-10` passes.

Blockers:

- Task 1 complete.

## Task 4: View-Model + Rendering Wiring

Deliver:

- Add step-aware operation-slot display token replacement.
- Add target slot expansion rendering via resolver contract.
- Implement highlight region generation for white-slot behavior.
- Ensure no step visuals when step key not on keypad.

Done criteria:

- UI scenarios `U-01` through `U-06` pass.
- Existing slot display behavior remains unchanged outside step mode.

Blockers:

- Tasks 1-3 complete.

## Task 5: Persistence and Parity Fixture Updates

Deliver:

- Update migration/schema/hydrate normalization for step fields.
- Update parity fixture payloads for new state/flag fields.
- Add persistence invalid-payload coercion coverage.

Done criteria:

- Integration scenarios `I-03` through `I-05` pass.
- No parity fixture schema mismatch failures.

Blockers:

- Tasks 1-4 complete.

## Task 6: Regression and Release Readiness Check

Deliver:

- Run focused step suites and full CI verification.

Required commands:

```bash
npm run build:web
node ./dist/tests/run-tests.js --grep="(reducer.input|operation-slot-display|persistence|uiIntegration)"
npm run ci:verify
```

Done criteria:

- All planned v0.8.2 scenarios pass.
- `npm run ci:verify` passes.
- No workflow changes required for Windows or Android release pipelines.

Blockers:

- Tasks 1-5 complete.

## 2. Handoff Gates

Implementation is ready to start when all are true:

- `docs/v0.8.2-execution-spec.md` has no unresolved decisions.
- `docs/v0.8.2-test-matrix.md` has suite mapping for every scenario ID.
- This backlog remains strictly ordered with explicit done criteria.

